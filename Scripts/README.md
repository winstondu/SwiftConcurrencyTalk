# Scripts

No Ruby tooling is required for this project.

The example targets and shared schemes are checked into `ConcurrencyInSwift.xcodeproj`. Add future examples through Xcode by creating a macOS Command Line Tool target with a matching shared scheme, then set target-level Swift settings as needed.
